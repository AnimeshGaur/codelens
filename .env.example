# CodeLens — Environment Variables (optional)
# API keys can also be entered directly in the UI when analyzing a project.

GEMINI_API_KEY=
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GROQ_API_KEY=

# Ollama (local LLM) — can also be configured from the UI
# OLLAMA_BASE_URL=http://localhost:11434/v1
